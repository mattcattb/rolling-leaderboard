# Changesets

Use changesets to manage versions and changelogs.

Create a release note:

```sh
bunx changeset
```

Apply versions:

```sh
bunx changeset version
```

Publish:

```sh
bunx changeset publish
```
